# Test fixtures

Place small, hand-authored KiCad projects here for parser and graph-query tests.

Fixtures should be minimal and should exercise one behaviour at a time, such as:

- a named net connecting an MCU pin to a resistor and transistor gate;
- a multi-pin component;
- unnamed local nets;
- component fields containing manufacturer and MPN information.
